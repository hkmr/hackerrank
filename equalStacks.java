class EqualStack {
    public static void main(String[] args) {
        // int s1 = 50, s2 = 40, s3 = 30;
        // int l1 = 10, l2 = 20, l3 = 30;
        // int grt = s1 > s2 ? (s1 > s3 ? s1 : s3) : s2 > s3 ? s2 : s3;
        // int sml = l1 < l2 ? (l1 < l3 ? l1 : l3) : l2 < l3 ? l2 : l3;
        // System.out.println(sml);
    }

    // public static int equalStack(int[] h1, int[] h2, int[] h3) {

    // }
}